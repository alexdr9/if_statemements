# --------- day 9

class musician:                                                     # creates a class where attributes can be created 
    
    def __init__(self, name, instrument, grade, is_in_truth):       # defines the class with its attributes
        self.name = name                                            # creates variables for each attribute within class
        self.instrument = instrument
        self.grade = grade
        self.is_in_truth = is_in_truth
